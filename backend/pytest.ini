[pytest]
DJANGO_SETTINGS_MODULE = core.settings
python_files = test_*.py
# Put the backend/ project root on sys.path so `core`, `futures`, `markets`,
# `accounts`, etc. import cleanly when pytest is invoked from backend/.
pythonpath = .
addopts = -q
